#define OneProgram

#define NoShipUser
#define BuildTool

PROGRAM = ivman

C++FILES = Main.c++ ClassDef.c++ Man.c++ Reader.c++ Writer.c++ BuildIssues.c++

OTHERSOURCE = manTree makeManDirs

TOP_OF_TREE = ../../..

#ifdef CreatingSgiMakefile
/*
 * When doing a 'make source', just create an ivman that is compiled
 * against the installed libInventor.so
 */
source: extrasource
extrasource: ivmaninst
	rm -f ivman
	ln ivmaninst ivman
#endif
